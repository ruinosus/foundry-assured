---
id: F04
title: ChangeSet local durável e concorrência otimista
type: AFK
deps: [F02]
user_stories: [US-003, US-010, US-016]
nordor_122: [auth, input-validation, logs, dados-sensiveis]
skills: [rnf-validation, novo-modulo]
---

## O que construir

Entregar o primeiro `OkfChangeSet` durável por API e UI contra SQLite, usando o mesmo contrato de repositório previsto para PostgreSQL. Criação idempotente, leitura e edição por nova revisão aplicam isolamento tenant-área, `If-Match`, hashes canônicos e regras no domínio, não no adapter.

## Stack e geração de código

- Skill: `novo-modulo` — usar apenas se a responsabilidade não couber nos módulos `okf` e de orquestração existentes; atualizar integralmente os contratos ADR-017.
- Skill: `rnf-validation` — aplicar conceitos de schema e erro, sem importar RNF Node.
- Frontend: tracer de criação, consulta e edição do ChangeSet com o Assured UI.
- Justificativa RNF: persistência é Python/SQLite e Python/PostgreSQL; não existe gerador RNF compatível neste projeto.

## Critérios de aceite (verificáveis)

- [ ] `POST/GET/PATCH /authoring/changesets` preserva estado após reinício e executa replay por `Idempotency-Key` sem duplicar agregado.
- [ ] Edição com `If-Match` obsoleto retorna `412` e não sobrescreve revisão concorrente.
- [ ] As mesmas provas contratuais rodam contra adapters SQLite e PostgreSQL substituíveis, sem regra de domínio no adapter.
- [ ] Leitura e escrita cruzadas entre tenant ou área falham fechadas.
- [ ] Banco, fixtures e respostas não contêm token, segredo ou credencial delegada.

## Definition of done

```bash
uv run --project apps/backend --no-sync python -m tests.okf.changeset_test
uv run --project apps/backend --no-sync python -m tests.tenancy.isolation_test
uv run --project apps/backend --no-sync python -m tests.architecture.importlinter_coverage_test
uv run --project apps/backend --no-sync python -m tests.smoke.routes_snapshot_test
cd apps/frontend && npm run lint
cd apps/frontend && npm run typecheck
cd apps/frontend && npm run build
```

## Checklist NORDOR-122

- [ ] Rotas exigem Reader/Author conforme operação e área autorizada.
- [ ] Documentos, headers e chaves de idempotência possuem schema e limites.
- [ ] SQL é parametrizado, paginado e indexado pelo escopo.
- [ ] Redaction acontece antes da persistência e do log.
- [ ] Erros de conflito e validação não vazam detalhes internos.
