---
id: F11
title: Pull request GitHub com identidade delegada
type: HITL
deps: [F10]
user_stories: [US-013, US-015, US-017]
nordor_122: [auth, input-validation, rate-limit, criptografia, logs, dados-sensiveis]
skills: [novo-modulo, rnf-validation, sdk-verify]
---

## O que construir

Entregar publicação GitHub da revisão aprovada por uma porta de domínio comum: Foundry Toolbox, servidor MCP oficial e OAuth identity passthrough criam branch, commit e pull request. Consentimento e aprovação nativa permanecem humanos; a aplicação não recebe nem persiste token.

## Stack e geração de código

- Skill: `sdk-verify` — verificar Learn, samples, MCP oficial e pacote instalado antes de fixar chamadas.
- Skill: `novo-modulo` — criar módulo de publicação somente se a responsabilidade ainda não tiver dono, com `public.py`/`internal/` e contratos completos.
- Skill: `rnf-validation` — aplicar validação/erros ao FastAPI Python, sem biblioteca RNF Node.
- Frontend: consentimento, progresso, PR e falhas explícitas no Assured UI.
- Justificativa RNF: integração e backend são Python; CLI RNF Node é incompatível.

## Critérios de aceite (verificáveis)

- [x] Somente Approver da área inicia publicação de ChangeSet aprovado com hash idêntico.
- [x] O adapter cria no máximo uma branch e um PR para a mesma chave de idempotência.
- [x] Conteúdo enviado corresponde ao conjunto aprovado por canonicalização declarada.
- [x] Token, header, URL de consentimento e resposta externa bruta não aparecem em banco, log, fixture ou OKF.
- [ ] Smoke autenticado prova branch, PR, consulta e replay idempotente em repositório de teste aprovado.

## Definition of done

```bash
uv run --project apps/backend --no-sync python scripts/gates.py -k publication
uv run --project apps/backend --no-sync python -m tests.architecture.importlinter_coverage_test
uv run --project apps/backend --no-sync python -m tests.smoke.routes_snapshot_test
cd apps/frontend && npm run lint
cd apps/frontend && npm run typecheck
cd apps/frontend && npm run build
# Com consentimento humano e ambiente de teste configurado:
uv run --project apps/backend --no-sync python -m tests.publication.github_smoke_test
```

## Checklist NORDOR-122

- [x] OAuth passthrough usa privilégio mínimo, consentimento e aprovação nativa.
- [x] Repositório, ref, caminhos, tamanho e conteúdo são validados antes da chamada externa.
- [x] Retry respeita `Retry-After`, limita três tentativas e só cobre falhas transitórias.
- [x] TLS protege trânsito e nenhum segredo é persistido ou logado.
- [x] Rate limit e timeout de 30 segundos limitam abuso e indisponibilidade.
- [x] Threat model existente cobre a nova fronteira GitHub.

## Evidências de conclusão

- DoD: quatro gates de publicação, import-linter, snapshot de rotas, lint, typecheck e build verdes.
- Gate global: 102/102 gates obrigatórios derivados do CI verdes; `--all` passou 121/123, com os dois extras alheios à F11 bloqueados pelo ambiente local (Redis real ausente e erro de I/O no daemon Docker).
- Smoke: o módulo infra-gated passou em skip limpo porque endpoint, repositório de teste e terminal interativo não estavam configurados; o smoke autenticado real permanece explicitamente pendente.
- MÁXIMA MAIOR: PASS; a implementação liga Foundry Toolbox, GitHub MCP oficial, OAuth identity passthrough e aprovação nativa do Agent Framework.
- Segurança: escrita marcada como `executing` antes do egress; resultado ambíguo exige intervenção e não sofre replay; projeções HTTP usam `no-store` e não expõem credenciais nem resposta externa bruta.
