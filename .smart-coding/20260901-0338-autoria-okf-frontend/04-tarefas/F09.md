---
id: F09
title: Conformidade honesta por fase
type: AFK
deps: [F03, F04, F07, F08]
user_stories: [US-011]
nordor_122: [auth, input-validation, logs]
skills: [rnf-validation]
---

## O que construir

Entregar execução e visualização de checks de conformidade por fase. Checks determinísticos executam de verdade; checks que dependem de Azure permanecem `pendente` no adapter local com motivo e evidência, sem fallback que converta ausência em aprovação.

## Stack e geração de código

- Skill: `rnf-validation` — modelar relatório imutável e estados aprovados, reprovados e pendentes no backend Python.
- Frontend: visão de conformidade por fase, severidade, evidência e bloqueio no Assured UI.
- Justificativa RNF: compor validadores existentes; CLI RNF Node não se aplica.

## Critérios de aceite (verificáveis)

- [x] `POST /authoring/changesets/{id}/validations` cria relatório imutável ligado à revisão e fase exatas.
- [x] Schema, referências, autorização declarada, invariantes e policies determinísticas têm evidência de execução real.
- [x] Check externo sem Azure fica pendente com motivo estável e nunca aprovado por simulação.
- [x] Check bloqueador reprovado impede a transição definida para a fase.
- [x] A UI distingue estado, evidência, cobertura e fonte sem depender de cor isoladamente.

## Definition of done

```bash
uv run --project apps/backend --no-sync python -m tests.okf.authoring_test
uv run --project apps/backend --no-sync python -m tests.okf.schema_test
uv run --project apps/backend --no-sync python -m tests.platform_ops.mcp_conformity_test
cd apps/frontend && npm run lint
cd apps/frontend && npm run typecheck
cd apps/frontend && npm run build
```

## Checklist NORDOR-122

- [x] Execução exige papel e área da fase correspondente.
- [x] Revisão, fase e parâmetros dos checks são validados e limitados.
- [x] Evidências e mensagens passam por redaction antes de resposta, persistência e log.
- [x] Falhas inesperadas retornam erro genérico e correlação investigável.

## Evidências de conclusão

- DoD: três suites backend, lint, typecheck e build verdes; `/compliance` presente no build.
- Gate global: 98/98 gates derivados do CI verdes.
- MÁXIMA MAIOR: PASS; MCP reutiliza `platform_ops.public.evaluate_mcp_binding` e Azure local permanece `pending`.
- UI: Playwright em 1440x900 e 390x844, estados vazio e preenchido sem overflow horizontal; estado, fonte, severidade, efeito, cobertura e evidência textuais.
- Segurança: relatórios isolados por tenant-área; matriz Author/Approver sem herança implícita; indisponibilidade externa permanece `pending` bloqueador na fase dependente.
