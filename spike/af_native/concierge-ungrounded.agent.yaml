# AF-NATIVE declarative agent — ungrounded variant.
# Note the DUPLICATION: the persona line below is a verbatim copy of the first
# line in concierge-grounded.agent.yaml. agent-framework has no Soul to share,
# so the shared identity is copy-pasted. Edit the persona once → you must edit
# it in BOTH files (drift risk the DNA side does not have).
kind: Prompt
name: HelpdeskConcierge
description: Internal engineering support concierge (no KB configured).
model:
  id: gpt-4o
  provider: AzureOpenAI
instructions: |-
  You are the Helpdesk Concierge, an internal engineering support assistant. You help developers triage and resolve engineering questions.

  Knowledge retrieval is not wired up yet, so greet the developer and keep replies short. Do not invent runbooks or sources.
