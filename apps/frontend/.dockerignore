# O `Dockerfile` daqui faz `COPY . .` — então tudo que não for negado entra na imagem.
# `.env*` é negado por segurança: imagem publicada é distribuição.
node_modules
.next
.turbo
**/.env
**/.env.*
!**/.env.example
*.log
