# Architecture gate — module boundaries checked in CI (ADR-017).
#
#     uv run lint-imports --config importlinter.toml
#
# All three contract families are live as of Phase 3: the layer rule, one privacy contract per
# module, and the independence that killed the core<->agents cycle.
#
# `allow_indirect_imports = true` on the privacy contracts is load-bearing: without it, every
# legitimate composition-root -> public -> internal chain is reported as a violation, because
# `forbidden` follows indirect imports by default. The rule we want is "no DIRECT import of
# another module's internal".

[tool.importlinter]
root_packages = ["app"]

# --- C1 — the three layers. composition may see everything; a module may see shared and other
# modules' public; shared may see nothing inside the app.
[[tool.importlinter.contracts]]
name = "Layers: composition > modules > shared"
type = "layers"
layers = [
    "app.main : app.registry",
    "app.modules",
    "app.shared",
]

# --- C2 — nothing imports the composition root.
[[tool.importlinter.contracts]]
name = "Nothing imports the composition root (app.main)"
type = "forbidden"
source_modules = ["app.modules", "app.shared"]
forbidden_modules = ["app.main"]

# --- C3 — the shared kernel imports no business module. This is the rule that makes the other
# layers mean anything: auth.py was split (ADR-017) precisely so it holds.
[[tool.importlinter.contracts]]
name = "Shared kernel imports no business module"
type = "forbidden"
source_modules = ["app.shared"]
forbidden_modules = ["app.modules"]

# --- C4 — the dead cycle, and only the direction that WAS the cycle.
# `independence` would forbid both directions, but only one was ever wrong: tenancy importing
# the MCP catalog to validate a connection kind. The reverse (platform_ops reading
# tenant_config for the per-tenant Foundry endpoint) is a legitimate module -> public edge and
# is in the measured graph. Forbidding it would be enforcing a rule nobody decided.
[[tool.importlinter.contracts]]
name = "tenancy does not import platform_ops (the dead cycle)"
type = "forbidden"
source_modules = ["app.modules.tenancy"]
forbidden_modules = ["app.modules.platform_ops"]

# --- C5 — internals are private. One contract per module; sources are every OTHER module plus
# the composition root, so a module can still reach its own internal.

[[tool.importlinter.contracts]]
name = "tenancy internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.tenancy.internal"]

[[tool.importlinter.contracts]]
name = "admin internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.admin.internal"]

[[tool.importlinter.contracts]]
name = "knowledge internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.knowledge.internal"]

[[tool.importlinter.contracts]]
name = "helpdesk internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.helpdesk.internal"]

[[tool.importlinter.contracts]]
name = "grounded internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.grounded.internal"]

[[tool.importlinter.contracts]]
name = "platform_ops internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.platform_ops.internal"]

[[tool.importlinter.contracts]]
name = "tickets internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.tickets.internal"]

[[tool.importlinter.contracts]]
name = "hosted internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.hosted.internal"]

[[tool.importlinter.contracts]]
name = "evaluation internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.evaluation.internal"]

[[tool.importlinter.contracts]]
name = "agentdefs internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.agentdefs.internal"]

[[tool.importlinter.contracts]]
name = "hitl internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.hitl.internal"]

# ADR-020: the LangGraph domain. Its framework's surface (create_agent, HumanInTheLoopMiddleware,
# the checkpointer) stays inside this module's internal/ — which is exactly the isolation a ports
# layer promised and did not need to provide.
[[tool.importlinter.contracts]]
name = "oncall internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.oncall.internal"]

# O módulo que expõe recursos do Foundry (agentes, e depois bases e skills) ao usuário final.
# O SDK da plataforma — AIProjectClient, AgentsOperations — fica dentro do internal/, pelo mesmo
# motivo dos demais: um upgrade da superfície do Foundry deve ser contido por construção, não
# por disciplina. É também o que mantém honesta a MÁXIMA MAIOR: se a chamada oficial vazasse
# para outros módulos, a "cola fina" viraria acoplamento espalhado.
[[tool.importlinter.contracts]]
name = "deepcall internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.deepcall.internal"]

# O módulo que expõe recursos do Foundry (agentes, e depois bases e skills) ao usuário final.
# O SDK da plataforma — AIProjectClient, AgentsOperations — fica dentro do internal/, pelo mesmo
# motivo dos demais: um upgrade da superfície do Foundry deve ser contido por construção, não
# por disciplina. É também o que mantém honesta a MÁXIMA MAIOR: se a chamada oficial vazasse
# para outros módulos, a "cola fina" viraria acoplamento espalhado.
[[tool.importlinter.contracts]]
name = "usecases internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.usecases.internal"]

# O módulo que expõe recursos do Foundry (agentes, e depois bases e skills) ao usuário final.
# O SDK da plataforma — AIProjectClient, AgentsOperations — fica dentro do internal/, pelo mesmo
# motivo dos demais: um upgrade da superfície do Foundry deve ser contido por construção, não
# por disciplina. É também o que mantém honesta a MÁXIMA MAIOR: se a chamada oficial vazasse
# para outros módulos, a "cola fina" viraria acoplamento espalhado.
[[tool.importlinter.contracts]]
name = "foundry internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.hitl",
    "app.modules.oncall",
    "app.modules.deepcall",
    "app.modules.usecases",
    "app.shared",
]
forbidden_modules = ["app.modules.foundry.internal"]

# As conversas. O `internal/` guarda o store (blobs de apêndice) e o `HistoryProvider` que os
# agentes recebem; de fora só se importa `public`. O motivo aqui é concreto: o caminho do blob
# começa no object-id do usuário, e é ele que faz o isolamento entre pessoas. Um módulo que
# montasse esse caminho por conta própria poderia escrever a conversa de alguém na pasta de
# outro — então quem monta o caminho é um lugar só, por construção.
[[tool.importlinter.contracts]]
name = "conversations internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.usecases",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.conversations.internal"]

# O propositor (ADR-022). O `internal/` guarda a montagem do prompt, o parse do rascunho e a
# chamada ao job de otimização do Foundry; de fora só se importa `public`. O motivo é a fronteira
# da própria ADR: publicar não é dele, e um módulo que alcançasse o interior dele poderia montar
# um caminho de escrita passando por baixo da superfície que o gate inspeciona.
[[tool.importlinter.contracts]]
name = "proposer internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.usecases",
    "app.modules.conversations",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.proposer.internal"]

# A trilha de auditoria (ADR-023). O `internal/` guarda o encadeamento por hash e o redator; de
# fora só se importa `public`. O motivo é a propriedade central: a trilha é append-only, e a
# superfície pública não expõe NADA que apague ou reescreva. Um módulo que alcançasse o interior
# poderia gravar um evento sem passar pelo encadeamento — e um evento fora da cadeia é
# indistinguível de um evento removido.
[[tool.importlinter.contracts]]
name = "audit internals are private"
type = "forbidden"
allow_indirect_imports = true
source_modules = [
    "app.modules.tenancy",
    "app.modules.admin",
    "app.modules.knowledge",
    "app.modules.helpdesk",
    "app.modules.grounded",
    "app.modules.platform_ops",
    "app.modules.tickets",
    "app.modules.hosted",
    "app.modules.evaluation",
    "app.modules.agentdefs",
    "app.modules.usecases",
    "app.modules.conversations",
    "app.modules.proposer",
    "app.modules.builder",
    "app.modules.hitl",
    "app.main",
    "app.registry",
    "app.shared",
]
forbidden_modules = ["app.modules.audit.internal"]
